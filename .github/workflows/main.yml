name: Recette Acceptance Etape 5
on: [push, workflow_dispatch]

jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - name: Recuperer le code
        uses: actions/checkout@v4

      - name: Executer le script de test
        run: |
          cp .env.example .env
          bash scripts/runtime_acceptance.sh
